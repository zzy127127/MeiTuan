# v2.0 验证记录

日期：2026-09-23。

## 自动验证

- `node --check app.js`、`node --check care.js`、`node --check engine.js`：语法检查通过。
- `node tests/engine.test.cjs`：统计、日期范围、独立舒适度、推荐、虚构示例断言通过。
- `node --test tests/app.test.cjs`：12 项应用回归通过，包含多标签页覆盖保护、草稿恢复、旧记录评分、存储失败提示、示例隔离及键盘焦点。

## 浏览器实际操作

- 未选择情绪保存：提示先选择，不新增记录。
- 选择焦虑、工作标签、舒适度和文字，保存后出现具有推荐原因的关怀选项。
- 呼吸练习开始、暂停、继续、提前结束、评分反馈和完成记录；洞察显示练习反馈。
- 日记搜索无结果、清除筛选；删除后撤销并刷新，记录保留。
- 导入旧版备份：缺失的舒适度保持未记录；修改文字后仍保持未知评分。
- 输入含 HTML 标签的日记：作为文字显示，不作为 HTML 执行。
- 虚构示例中切换 7 天 / 30 天：统计范围与显示数量一致。
- 音乐筛选仅呈现音乐活动；播放和暂停状态正确；关闭练习不新增完成记录。
- 手机宽度 390px：页面无横向溢出，导航图标 19px；桌面导航图标 20px。

浏览器测试使用 localhost 上的虚构测试记录，未将真实日记上传仓库。测试验证功能与呈现，不证明自助练习具备临床疗效。
